{{>location}}
<section class="row main-quote"> 
    {{>quote}}
</section>
<div class="container-fluid main-inner-wrapper row">
    <main class="col-md-8">
        {{>blogPost post}}
    </main>
    <aside class="col-md-4">
        {{>list quotes}}
        {{>list archives}}
        {{>list recentPosts}}
    </aside>
</div>
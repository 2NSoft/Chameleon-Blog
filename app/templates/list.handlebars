<article class="col-sm-3 wrapper-list">
    <h3 class='list-heading'>{{type}}</h3>
    {{#unless skipList}}
        <ul>
        {{#each items}}
            <li><a href="/blog/{{_id}}" data-navigo>{{text}}</a></li>
        {{/each}}
        </ul>
    {{else}}
        <p>
            <a href="/blog/{{_id}}" data-navigo>
                {{text}}
            </a>
        </p>
    {{/unless}}
</article>